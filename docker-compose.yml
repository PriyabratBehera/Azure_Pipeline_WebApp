services:

  app:
    build:
      context: .
      dockerfile: Dockerfile

    ports:
      - "5001:5000"

    environment:
      APP_ENV: Docker
      SERVER_NAME: Docker Container

    restart: unless-stopped